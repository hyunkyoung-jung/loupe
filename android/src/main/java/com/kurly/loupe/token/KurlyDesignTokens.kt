package com.kurly.loupe.token

/**
 * 기본 디자인 토큰 (빈 목록).
 *
 * 프로젝트의 디자인 시스템에 맞는 토큰은
 * [com.kurly.loupe.DesignInspector.init]의 `colorTokens` 파라미터로 주입하세요.
 */
internal object KurlyDesignTokens {
    fun allTokens(): List<ColorToken> = emptyList()
}
